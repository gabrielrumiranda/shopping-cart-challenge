language: ruby
rvm:
  - 2.6.0
before_install:
  - gem update --system
  - gem install bundler
  - gem install rails
  - bundle install
env:
  - CODECOV_TOKEN=045ddea2-5388-4b2b-835b-f973e0e3d391
script:
  - bundle exec rake spec
after_success:
  - codecov
